<Page x:Class="MakeExcel.Pages.mainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MakeExcel.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="500"
      Title="mainPage">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="2">
            <Button Click="MakeExcel" Name="btnExcel" Style="{StaticResource btnStyle}" Content="Создать Excel"/>
            <Button Click="EditPath" Name="btnPath" Style="{StaticResource btnStyle}" Content="Путь к файлам"/>
            <Button Click="ShowLog" Name="btnLog"  Style="{StaticResource btnStyle}" Content="Просмотр логов"/>
        </StackPanel>
    </Grid>
</Page>
